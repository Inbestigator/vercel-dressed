{
  "scripts": {
    "dev": "pnpm build -i && pnpm start",
    "register": "pnpm build -r && pnpm start",
    "build": "pnpx deno -A jsr:@dressed/cmd build",
    "start": "pnpx tsx bot.gen.ts",
    "bundle": "pnpx esbuild bot.gen.ts > bot.bundle.js --bundle --minify --platform=node"
  },
  "dependencies": {
    "@dressed/dressed": "npm:@jsr/dressed__dressed@^1.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}

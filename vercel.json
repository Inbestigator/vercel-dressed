{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm build-bot -i && pnpm bundle",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}

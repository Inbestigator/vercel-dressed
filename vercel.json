{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm build -i && pnpm bundle",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}

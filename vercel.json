{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm build && pnpm bundle",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}

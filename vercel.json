{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm build -i && pnpm bundle",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}
